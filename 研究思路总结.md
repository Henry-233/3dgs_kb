
### 基线现状

WildGS-SLAM（CVPR 2025）在单目动态场景下是当前 SOTA，核心机制是 DINOv2 特征 + 在线 MLP 预测逐像素不确定性，用于 tracking BA 降权和 mapping loss 降权。

复现结果：MoCap 平均 ATE 0.47 cm（与论文一致），Bonn 平均 PSNR 18.50 dB、ATE 2.47 cm。

---

### 已验证失败的方向

**路线1：帧间 DINO 时序先验 warmup（V1 warp 版 + V2 同位置版）**

根本矛盾：任何依赖渲染结果的信号在序列初期都是噪声，而计算先验本身又依赖不可靠的初期位姿和渲染，形成循环依赖。V1 在 table_tracking2 ATE 暴涨 47%，V2 收窄到 +10% 但仍未超越 baseline。

**路线2：渲染误差 EMA 动态高斯剔除**

根本矛盾：EMA 无法区分"动态导致的误差"和"重建不完整导致的误差"，两类高斯误差同步衰减。实际上 EMA 剔除从未触发（阈值 0.3 在 table_tracking2 始终未被超过），table_tracking2 ATE 恶化 18%。

诊断发现：table_tracking2 的 ATE 问题根源在 tracking 侧（光流在高动态占比场景下不准），不在地图污染，对地图的任何干预边际收益极低。

---

### 当前进行中的方向

**路线3：VarSplat 风格高斯内建不确定性（进行中）**

核心思路：给每个高斯加可学习外观方差 σ²，通过全方差定律和 alpha compositing 渲染逐像素不确定性图 V，用 V 改进 mapping loss 加权并训练 NLL loss。

与路线1/2的本质区别：σ² 从高斯几何自然涌现，不依赖渲染误差的准确性，从第一帧就有意义，天然绕开冷启动矛盾。

目标：提升 Bonn PSNR/SSIM，目标序列是 person_tracking 和 crowd。

实现状态：代码已完成，实验运行中。

---

### 文献支撑的其他改进方向

以下是从简报和竞品分析中提炼的、**尚未尝试但有文献依据**的方向，按可行性排序：

---

**方向 A：自适应阈值替代固定阈值（最轻量）**

来源：RoGER-SLAM 的条件触发思路。

现有 WildGS-SLAM 的不确定性权重截断是固定的（`weights < 0.1 → 置零`），不同序列误差分布差异很大，固定阈值在高动态场景过于激进。

改进：用当前帧误差的自适应统计量替代固定阈值：

```python
threshold = mean_error + k * std_error  # 动态阈值
```

改动 3 行，零风险，适合作为 VarSplat 的补充实验。

---

**方向 B：渲染一致性检测动态区域（ADD-SLAM 思路的单目适配）**

来源：ADD-SLAM 去掉动态检测后 ATE 从 2.77 暴涨到 52.5 cm，说明渲染 vs 观测差异是极强的动态信号。

ADD-SLAM 是 RGB-D，用几何+颜色差异检测。单目适配版：只用颜色差异（渲染图 vs GT 图），用 SSIM 逐块计算不一致性，对不一致区域抑制 densify。

与路线2的区别：不删除已有高斯，而是抑制新高斯在不一致区域生长，从源头减少污染。

---

**方向 C：双轨制 tracking 信号（TVG-SLAM 思路的轻量版）**

来源：TVG-SLAM 用 DUST3R 几何约束作为备用 tracking 通路，Cambridge ATE 下降 69%。

完整版太重（400ms/帧），但核心思想可以轻量化：在 tracking loss 中加入一个不依赖地图质量的几何约束项——用 Metric3D 的单目深度估计做帧间深度一致性约束，地图不可靠时几何约束权重自动升高。

预期对 table_tracking2 这类 tracking 侧问题有效，但改动范围较大。

---

**方向 D：限制表示容量强制解耦（Taming the Light 思路）**

来源：Taming the Light 把颜色压到 64 色，迫使光照变化归因到光照因子而非材质。

在动态场景中类比应用：限制动态高斯的 SH 阶数（只用 0 阶，不允许视角相关颜色），迫使动态物体无法精细拟合，opacity 更快被压低。

改动局部，逻辑清晰，但效果不确定。

---

### 优先级建议

```
当前进行中：路线3（VarSplat）→ 验证 Bonn PSNR 提升
           ↓ 如果有效
补充实验：  方向 A（自适应阈值）→ 叠加改进
           ↓ 如果 ATE 仍有提升空间
备选方向：  方向 B（渲染一致性抑制 densify）→ 针对 tracking 侧问题
```

方向 C 和 D 作为论文 future work 或 discussion 使用，不建议花大量实验时间。